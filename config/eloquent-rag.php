<?php

declare(strict_types=1);

return [
    /*
    |--------------------------------------------------------------------------
    | Placeholder configuration
    |--------------------------------------------------------------------------
    |
    | This file is intentionally empty scaffolding. Real configuration keys
    | (chunking defaults, embedding model/dimensions, queue connection,
    | batch size) land in Phase 1+ per docs/spikes/0001-dependency-graph.md
    | and the ADRs in docs/adr/.
    |
    */
];
